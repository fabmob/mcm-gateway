ARG MAVEN_IMAGE_NAME

FROM ${MAVEN_IMAGE_NAME}

COPY target/routing-api*.jar /routing-api.jar
COPY elastic-apm-agent-1.35.0.jar /elastic-apm-agent-1.35.0.jar

EXPOSE 8082

CMD java -javaagent:./elastic-apm-agent-1.35.0.jar -jar routing-api.jar
